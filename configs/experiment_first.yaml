experiment_name: first_experiment
seeds: [0, 1, 2, 3, 4]
groups:
  - full_shaping
  - no_shaping
  - random
  - greedy
  - hybrid_deepseek_greedy
total_timesteps: 300000
eval_interval: 10000
eval_episodes: 20
output_dir: outputs/first_experiment
env_configs:
  full_shaping: configs/env_full_shaping.yaml
  no_shaping: configs/env_no_shaping.yaml
ppo:
  n_steps: 512
  batch_size: 64
  verbose: 0
